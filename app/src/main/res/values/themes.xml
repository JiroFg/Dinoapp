<resources>
    <!-- Base application theme. -->
    <style name="Theme.Dinoapp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/jurassic_park</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.Dinoapp</item>
    </style>

    <style name="ThemeOverlay.Dinoapp.FullscreenContainer" parent="">
    </style>

    <style name="MyAlertDialogStyle" parent="android:Theme.Material.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="colorAccent">@color/green</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">@color/green</item>
        <item name="android:textColorSecondary">@color/green</item>

        <!-- Used for the background -->
        <item name="android:colorPrimary">@color/green</item>
        <item name="android:colorAccent">@color/green</item>
    </style>

</resources>